export enum UrlSlugType {
  // Public url
  LOGIN = "/log-in",
  REGISTER = "/register",
  FORGOT_PASSWORD = "/forgot-password",

  // User Url
  HOME = "/home",
  ORDERS = "/orders",
  MENU = "/menu",
  PAYMENTS = "/payments",
  TEAM = "/team",
  SETTINGS = "/settings",
}
