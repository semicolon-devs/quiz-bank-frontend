export enum UserRole {
  ADMIN = "ADMIN",
  MODERATOR = "MODERATOR",
  USER = "USER",
  LMS_USER = "LMS_USER",
}

export enum PaperType {
  ONE_ATTEMPT = "ONE ATTEMPT",
  MULTIPLE_ATTEMPT = "MULTIPLE ATTEMPT",
}
